// import Vue from 'vue'
// import Weather from '../../../src/components/weather/Weather'

describe('Weather', () => {
  /* let vm =
    beforeEach(() => {
      vm = new Vue({
        data: Weather.data,
        template: '<div><test></test></div>',
        components: {
          'test': Weather
        }
      }).$mount()
    })

  it('get day of the week', () => {
    expect(vm.city).to.be.equal('')
  }) */
  it('should render correct contents', () => {
    // const Constructor = Vue.extend(Weather)
    // const vm = new Constructor().$mount()
    console.log('data')
    // console.log(vm.data)
    expect('1')
      .to.equal('1')
  })
})
